setup:
  config:
    VITE_API_URL: https://osakana-calendar-api-7fca63533648.herokuapp.com
    NODE_ENV: production

build:
  docker:
    web:
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}

run:
  web: node server.js